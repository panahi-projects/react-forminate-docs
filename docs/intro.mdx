---
sidebar_position: 1
---
import { Code1 } from "@site/src/components/Codes"

<div align="center">
        <a href="#" title="React-Forminate">
            <img src="/img/React-Forminate-Logo-256x256-bg-transparent.webp" width="300" alt="React Forminate Header" />
        </a>
    # React-Forminate
</div>
---

The missing link between **configuration** and **complex forms** in React

<Code1 
  description="Basic form example using React-Forminate with a text input field, live validation, and form submission handling."
  features={[
        "DynamicForm component usage",
        "Text input field configuration",
        "Form submission handling",
        "Real-time validation",
        "Console logging of form values",
        "Basic form structure"
      ]}
  keywords={[
        "react form example",
        "DynamicForm demo",
        "React-Forminate tutorial",
        "form implementation",
        "text input field",
        "form submission",
        "react form library",
        "form validation"
      ]} />

### Why This Exists
Most form libraries force you to choose:
- Simple setup (but limited features)
- Full control (with complex boilerplate)

React Forminate gives you both through:
1. A declarative JSON schema
2. Escape hatches for custom logic

### How It Feels to Use
1. Define your form structure in JSON/TypeScript
2. Render with `<DynamicForm>`
3. Extend with plugins when needed

<pre>
  <code>
    <span style={{ color: '#6ee7b7' }}># The moment you’ll fall in love:</span>
    npm install <span style={{ color: '#f472b6' }}>react-forminate</span>
    <span style={{ color: '#6ee7b7' }}># Then watch 100 lines of form code shrink to 10</span>
  </code>
</pre>
